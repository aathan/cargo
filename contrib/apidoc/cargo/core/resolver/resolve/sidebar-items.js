window.SIDEBAR_ITEMS = {"enum":[["ResolveVersion","A version to indicate how a `Cargo.lock` should be serialized. Currently V2 is the default when creating a new lockfile. If a V1 lockfile already exists, it will stay as V1."]],"struct":[["Resolve","Represents a fully-resolved package dependency graph. Each node in the graph is a package and edges represent dependencies between packages."]]};